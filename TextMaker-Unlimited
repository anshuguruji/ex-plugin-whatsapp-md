/* Codded by AnshuGuruji
Unlimited API for Photooxy, Textpro and Instagram scraper.
Material: https://www.npmjs.com/package/textmaker-thiccy
Github: https://github.com/anshuguruji/TextMaker-Unlimited
This code works with unlimited and completely free an API scraper.
Don't use it for illegal purposes.
*/
const { bot, textMaker } = require('../lib')

bot(
	{
		pattern: 'sed ?(.*)',
		fromMe: true,
		desc: 'Sad text effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://en.ephoto360.com/write-text-on-wet-glass-online-589.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'steel ?(.*)',
		fromMe: true,
		desc: 'Steel text effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match)
			return await message.sendMessage(
				'Give me text\nExample .steel steel;effect'
			)
		const [text1, text2] = match.split(';')
		if (!text1 || !text2)
			return await message.sendMessage(
				'Give me text\nExample .steel steel;effect'
			)
		const effect_url = 'https://en.ephoto360.com/steel-text-effect-66.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)

bot(
	{
		pattern: 'metallic ?(.*)',
		fromMe: true,
		desc: 'Matallic text effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://textpro.me/create-a-metallic-text-effect-free-online-1041.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'glitch ?(.*)',
		fromMe: true,
		desc: 'Glitch text effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match)
			return await message.sendMessage(
				'Give me text\nExample .glitch steel;effect'
			)
		const [text1, text2] = match.split(';')
		if (!text1 || !text2)
			return await message.sendMessage(
				'Give me text\nExample .glitch steel;effect'
			)
		const effect_url =
			'https://textpro.me/create-glitch-text-effect-style-tik-tok-983.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)

bot(
	{
		pattern: 'burn ?(.*)',
		fromMe: true,
		desc: 'Matallic text effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://photooxy.com/logo-and-text-effects/write-text-on-burn-paper-388.html'
		const { url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)

//Example for text effect with two input
bot(
	{
		pattern: '8bit ?(.*)',
		fromMe: true,
		desc: 'Glitch text effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match)
			return await message.sendMessage(
				'Give me text\nExample .glitch steel;effect'
			)
		const [text1, text2] = match.split(';')
		if (!text1 || !text2)
			return await message.sendMessage(
				'Give me text\nExample .glitch steel;effect'
			)
		const effect_url =
			'https://photooxy.com/logo-and-text-effects/8-bit-text-on-arcade-rift-175.html'
		const { url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'textdevil ?(.*)',
		fromMe: true,
		desc: 'Makes Devil Themed Logo',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =  
			'https://textpro.me/create-neon-devil-wings-text-effect-online-free-1014.html'
		const { url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
  )
  bot(
	{
		pattern: 'textbear ?(.*)',
		fromMe: true,
		desc: 'Makes Logo With Bear Icon',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =  
			'https://textpro.me/online-black-and-white-bear-mascot-logo-creation-1012.html'
		const { url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
  )
  bot(
	{
		pattern: 'textneon ?(.*)',
		fromMe: true,
		desc: 'Makes Logo With Neon Effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://textpro.me/create-a-futuristic-technology-neon-light-text-effect-1006.html'
		const { url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'textsci ?(.*)',
		fromMe: true,
		desc: 'Makes science ficition Effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://textpro.me/create-science-fiction-text-effect-online-free-1038.html'
		const { url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'textleaf ?(.*)',
		fromMe: true,
		desc: 'Makes dark green Effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://en.ephoto360.com/dark-green-typography-online-359.html'
		const { url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'textmatrix ?(.*)',
		fromMe: true,
		desc: 'Makes matrix text Effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://en.ephoto360.com/matrix-text-effect-154.html'
		const { url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'text2gradient ?(.*)',
		fromMe: true,
		desc: 'Makes 3d gradient text Effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://en.ephoto360.com/create-3d-gradient-text-effect-online-600.html'
		const { url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'textballoon ?(.*)',
		fromMe: true,
		desc: 'Makes your text on hotair ballon ',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://en.ephoto360.com/writing-your-name-on-hot-air-balloon-506.html'
		const { url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'textsnow ?(.*)',
		fromMe: true,
		desc: 'Makes snow effect on test',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://en.ephoto360.com/snow-on-text-online-107.html'
		const { url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'text2gold ?(.*)',
		fromMe: true,
		desc: 'Makes gold text Effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://en.ephoto360.com/gold-text-effect-pro-271.html'
		const { url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'text5graf ?(.*)',
		fromMe: true,
		desc: 'Makes text graffiti Effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://en.ephoto360.com/graffiti-color-199.html'
		const { url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'text3fire ?(.*)',
		fromMe: true,
		desc: 'Makes dragon fire text Effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://en.ephoto360.com/dragon-fire-text-effect-111.html'
		const { url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'text2space ?(.*)',
		fromMe: true,
		desc: 'Makes Logo with galaxy style Effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://en.ephoto360.com/create-galaxy-style-free-name-logo-438.html'
		const { url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'text3space ?(.*)',
		fromMe: true,
		desc: 'Makes Logo galaxy text Effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://en.ephoto360.com/galaxy-text-effect-116.html'
		const { url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'textgold ?(.*)',
		fromMe: true,
		desc: 'Makes text With gold Effect 2',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url = 'https://en.ephoto360.com/modern-gold-3-212.html'
		const { url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'text3space ?(.*)',
		fromMe: true,
		desc: 'Makes text with space effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://en.ephoto360.com/galaxy-text-effect-116.html'
		const { url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'textangel ?(.*)',
		fromMe: true,
		desc: 'Makes text Angel Wing Effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://en.ephoto360.com/wings-galaxy-206.html'
		const { url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'text2pink ?(.*)',
		fromMe: true,
		desc: 'Makes Logo With blackpink Effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://en.ephoto360.com/create-a-blackpink-neon-logo-text-effect-online-710.html'
		const { url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'textabear ?(.*)',
		fromMe: true,
		desc: 'Makes text bear Animation',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://en.ephoto360.com/create-funny-animations-of-a-traveling-bear-701.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'text6neon ?(.*)',
		fromMe: true,
		desc: 'makes text lightgreen neon effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://en.ephoto360.com/create-light-effects-green-neon-online-429.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'textheart ?(.*)',
		fromMe: true,
		desc: 'heart wings text gifs',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://en.ephoto360.com/write-name-on-heart-with-wings-gifs-430.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'text4graf ?(.*)',
		fromMe: true,
		desc: 'Graffiti text effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://en.ephoto360.com/text-graffiti-3d-208.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'textpuppy ?(.*)',
		fromMe: true,
		desc: 'Cute puppy text effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://en.ephoto360.com/create-puppy-cute-animated-online-478.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'textroad ?(.*)',
		fromMe: true,
		desc: 'Glowing text effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://en.ephoto360.com/create-glowing-text-effects-online-706.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'textsad ?(.*)',
		fromMe: true,
		desc: 'Sad text effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://en.ephoto360.com/write-text-on-wet-glass-online-589.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'text3pubg ?(.*)',
		fromMe: true,
		desc: 'pubg cover text effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://en.ephoto360.com/create-the-cover-game-playerunknown-s-battlegrounds-401.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'text2pubg ?(.*)',
		fromMe: true,
		desc: 'Pubg glitch logo text effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://en.ephoto360.com/create-pubg-style-glitch-video-avatar-554.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'textpubg ?(.*)',
		fromMe: true,
		desc: 'pubg logo text effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://en.ephoto360.com/lightning-pubg-video-logo-maker-online-615.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'text2devil ?(.*)',
		fromMe: true,
		desc: 'Sad text effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://textpro.me/create-green-horror-style-text-effect-online-1036.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'texthub ?(.*)',
		fromMe: true,
		desc: 'hub text effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match)
			return await message.sendMessage(
				'Give me text\nExample .texthub tex1;tex2'
			)
		const [text1, text2] = match.split(';')
		if (!text1 || !text2)
			return await message.sendMessage(
				'Give me text\nExample .texthub msg1;msg2'
			)
		const effect_url =
			'https://textpro.me/pornhub-style-logo-online-generator-free-977.html'
		const { url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'text3graf ?(.*)',
		fromMe: true,
		desc: 'graffiti text logo',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://textpro.me/break-wall-text-effect-871.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'textretro ?(.*)',
		fromMe: true,
		desc: 'Video game text effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match)
			return await message.sendMessage(
				'Give me text\nExample .textretro tex1;tex2'
			)
		const [text1, text2] = match.split(';')
		if (!text1 || !text2)
			return await message.sendMessage(
				'Give me text\nExample .textretro msg1;msg2'
			)
		const effect_url =
			'https://textpro.me/video-game-classic-8-bit-text-effect-1037.html'
		const { url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'textberry ?(.*)',
		fromMe: true,
		desc: 'Berry text effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://textpro.me/create-berry-text-effect-online-free-1033.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'text2fire ?(.*)',
		fromMe: true,
		desc: 'Halloween fire text effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://textpro.me/halloween-fire-text-effect-940.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'textpink ?(.*)',
		fromMe: true,
		desc: 'Pink text effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://textpro.me/blood-text-on-the-frosted-glass-941.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'textblood ?(.*)',
		fromMe: true,
		desc: 'Horror Blood text effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://textpro.me/horror-blood-text-effect-online-883.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'text2light ?(.*)',
		fromMe: true,
		desc: 'Thunder light text effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://textpro.me/online-thunder-text-effect-generator-1031.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'textart ?(.*)',
		fromMe: true,
		desc: 'Art text multicolur effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://textpro.me/online-multicolor-3d-paper-cut-text-effect-1016.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'textwcolor ?(.*)',
		fromMe: true,
		desc: 'Sad text effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://textpro.me/create-a-free-online-watercolor-text-effect-1017.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'textmagma ?(.*)',
		fromMe: true,
		desc: 'Magma text effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://textpro.me/create-a-magma-hot-text-effect-online-1030.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'textmetal ?(.*)',
		fromMe: true,
		desc: 'Metal text effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://textpro.me/create-a-3d-glossy-metal-text-effect-1019.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'textpaper ?(.*)',
		fromMe: true,
		desc: 'paper text effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://textpro.me/create-art-paper-cut-text-effect-online-1022.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'textbglass ?(.*)',
		fromMe: true,
		desc: 'Broken Glass text effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://textpro.me/broken-glass-text-effect-free-online-1023.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'textstone ?(.*)',
		fromMe: true,
		desc: 'Stone text effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://textpro.me/3d-stone-cracked-cool-text-effect-1029.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'textwolf ?(.*)',
		fromMe: true,
		desc: 'Wolf text logo effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match)
			return await message.sendMessage(
				'Give me text\nExample .textwolf text1;text2'
			)
		const [text1, text2] = match.split(';')
		if (!text1 || !text2)
			return await message.sendMessage(
				'Give me text\nExample .textwolf tex1;text2'
			)
		const effect_url =
			'https://textpro.me/create-wolf-logo-galaxy-online-936.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'textmarvel ?(.*)',
		fromMe: true,
		desc: '3dMarvel text logo effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match)
			return await message.sendMessage(
				'Give me text\nExample .textmarvel text1;text2'
			)
		const [text1, text2] = match.split(';')
		if (!text1 || !text2)
			return await message.sendMessage(
				'Give me text\nExample .textmarvel tex1;text2'
			)
		const effect_url =
			'https://textpro.me/create-logo-style-marvel-studios-online-971.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'text2marvel ?(.*)',
		fromMe: true,
		desc: '3dAvengers Marvel text logo effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match)
			return await message.sendMessage(
				'Give me text\nExample .text2marvel text1;text2'
			)
		const [text1, text2] = match.split(';')
		if (!text1 || !text2)
			return await message.sendMessage(
				'Give me text\nExample .text2marvel tex1;text2'
			)
		const effect_url =
			'https://textpro.me/create-3d-avengers-logo-online-974.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'textlion ?(.*)',
		fromMe: true,
		desc: 'LION text logo effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match)
			return await message.sendMessage(
				'Give me text\nExample .textlion text1;text2'
			)
		const [text1, text2] = match.split(';')
		if (!text1 || !text2)
			return await message.sendMessage(
				'Give me text\nExample .textlion tex1;text2'
			)
		const effect_url =
			'https://textpro.me/create-lion-logo-mascot-online-938.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'textwall ?(.*)',
		fromMe: true,
		desc: 'Cool wall text logo effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match)
			return await message.sendMessage(
				'Give me text\nExample .textwall text1;text2'
			)
		const [text1, text2] = match.split(';')
		if (!text1 || !text2)
			return await message.sendMessage(
				'Give me text\nExample .textwall tex1;text2'
			)
		const effect_url =
			'https://textpro.me/create-cool-wall-graffiti-text-effect-online-1009.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'text2wall ?(.*)',
		fromMe: true,
		desc: 'cool wall text logo2 effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match)
			return await message.sendMessage(
				'Give me text\nExample .text2wall text1;text2'
			)
		const [text1, text2] = match.split(';')
		if (!text1 || !text2)
			return await message.sendMessage(
				'Give me text\nExample .text2wall tex1;text2'
			)
		const effect_url =
			'https://textpro.me/create-a-cool-graffiti-text-on-the-wall-1010.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'textglitch ?(.*)',
		fromMe: true,
		desc: 'Glitch text logo effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match)
			return await message.sendMessage(
				'Give me text\nExample .textglitch text1;text2'
			)
		const [text1, text2] = match.split(';')
		if (!text1 || !text2)
			return await message.sendMessage(
				'Give me text\nExample .textglitch tex1;text2'
			)
		const effect_url =
			'https://textpro.me/create-glitch-text-effect-style-tik-tok-983.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'textjoker ?(.*)',
		fromMe: true,
		desc: 'Joker text logo effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://textpro.me/create-logo-joker-online-934.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'textglitter ?(.*)',
		fromMe: true,
		desc: 'AdvancedGlitter text effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://textpro.me/advanced-glow-text-effect-873.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'textbokeh ?(.*)',
		fromMe: true,
		desc: 'Bokeh text effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://textpro.me/bokeh-text-effect-876.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'text3neon ?(.*)',
		fromMe: true,
		desc: 'Neon3 text effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://textpro.me/neon-text-effect-online-963.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'textice ?(.*)',
		fromMe: true,
		desc: 'Ice text effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://textpro.me/ice-cold-text-effect-862.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'textsmoke ?(.*)',
		fromMe: true,
		desc: 'Smoke text effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://photooxy.com/other-design/create-an-easy-smoke-type-effect-390.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'textfire ?(.*)',
		fromMe: true,
		desc: 'Fire text effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://photooxy.com/logo-and-text-effects/realistic-flaming-text-effect-online-197.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'textcup ?(.*)',
		fromMe: true,
		desc: 'Cup text effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://photooxy.com/logo-and-text-effects/put-text-on-the-cup-387.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'textharry ?(.*)',
		fromMe: true,
		desc: 'Harry Potter text effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://photooxy.com/logo-and-text-effects/create-harry-potter-text-on-horror-background-178.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'text4neon ?(.*)',
		fromMe: true,
		desc: 'Neon4 text effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://photooxy.com/logo-and-text-effects/illuminated-metallic-effect-177.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'textgamelogo ?(.*)',
		fromMe: true,
		desc: 'Gamelogo text effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://en.ephoto360.com/free-gaming-logo-maker-for-fps-game-team-546.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'textmshield ?(.*)',
		fromMe: true,
		desc: 'mascot shield logo text effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url = 'https://en.ephoto360.com/create-mascot-shield-logo-online-for-free-758.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
			'
	}
)
bot(
	{
		pattern: 'texthb ?(.*)',
		fromMe: true,
		desc: 'Happy Birthday text effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url = 'https://en.ephoto360.com/romantic-flower-heart-birthday-cake-by-name-edit-466.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'texthbchoco ?(.*)',
		fromMe: true,
		desc: 'Happy Birthday Chocolate text effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://en.ephoto360.com/chocolate-birthday-cake-with-candle-with-name-generator-461.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'textwings ?(.*)',
		fromMe: true,
		desc: 'Heart Wings text effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://en.ephoto360.com/write-name-on-heart-with-wings-gifs-430.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'textadglow ?(.*)',
		fromMe: true,
		desc: 'Advanced Glow text effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://en.ephoto360.com/advanced-glow-effects-74.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'logowings ?(.*)',
		fromMe: true,
		desc: ' Make wings text effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://en.ephoto360.com/create-colorful-angel-wing-avatars-731.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'logobear ?(.*)',
		fromMe: true,
		desc: ' Make bear logo text effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://en.ephoto360.com/free-bear-logo-maker-online-673.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'logodragon ?(.*)',
		fromMe: true,
		desc: ' Make Dragon logo text effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://en.ephoto360.com/create-elegant-rotation-logo-online-586.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'logovideo ?(.*)',
		fromMe: true,
		desc: ' Make logo intro video text effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://en.ephoto360.com/free-logo-intro-video-maker-online-558.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'logometal ?(.*)',
		fromMe: true,
		desc: ' Make Metal Mascots logo text effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://en.ephoto360.com/metal-mascots-logo-maker-486.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'logo3d ?(.*)',
		fromMe: true,
		desc: ' Make 3d Mascots logo text effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://en.ephoto360.com/make-team-logo-online-free-432.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'logosline ?(.*)',
		fromMe: true,
		desc: ' Make Stylish line logo text effect',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://en.ephoto360.com/metal-logo-online-108.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
bot(
	{
		pattern: 'logotigervideo ?(.*)',
		fromMe: true,
		desc: ' Make 3d lion logo intro video',
		type: 'textmaker',
	},
	async (message, match) => {
		if (!match) return await message.sendMessage('Give me text')
		const effect_url =
			'https://en.ephoto360.com/create-digital-tiger-logo-video-effect-723.html'
		const { status, url } = await textMaker(effect_url, match)
		if (url) return await message.sendFromUrl(url)
	}
)
